import React from "react";

export default function SetProgramRegistersInstruction() {
  return (
    <>
      <p>
        Click on a programming card (Move 1, Rotate, etc.) and click on a
        register to set it. You can click them in any order.
      </p>
      <p>
        If you change your mind about a register, you can either set a new
        instruction or clear it by clicking on it twice (first click selects,
        second click clears)
      </p>
    </>
  );
}
